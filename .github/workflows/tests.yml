name: Tests

on:
  push:
    branches: [main, master]
  pull_request:

concurrency:
  group: tests-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.13"

      - name: Install ruff
        run: pip install ruff

      - name: Lint
        run: python -m ruff check custom_components/ tests/

  test:
    # Tests run against a real Home Assistant (pytest-homeassistant-custom-component),
    # which requires the Python version current Home Assistant needs.
    runs-on: ubuntu-latest
    needs: lint

    steps:
      - uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.14"

      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install -r requirements_test.txt
          python -c "import json; print('\n'.join(json.load(open('custom_components/niimbot/manifest.json'))['requirements']))" > requirements.txt
          pip install -r requirements.txt
          python scripts/ha_component_requirements.py bluetooth bluetooth_adapters recorder > requirements_ha.txt
          pip install -r requirements_ha.txt

      - name: Run tests
        run: python -m pytest tests/ -v --timeout 120
